#ifndef FRACTALBUILDER_H#define FRACTALBUILDER_H#include "CycledLetterSequence.h"#include <string>using std::string;class FractalBuilder{public:	FractalBuilder();	FractalBuilder(int width, int height);	FractalBuilder(int width, int height, string destination);	void build();	private:	int width;	int height;	string destination;	CycledLetterSequence S;		double lyapunovExponent(int N, double a, double b, CycledLetterSequence S);};#endif